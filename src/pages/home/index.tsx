import styles from "./homepage.module.css";

const Home = () => {
  return (
    <section className={styles.sectionField}>
      <h2 className={styles.headline}>This the home vite prehost</h2>
    </section>
  );
};
export default Home;
